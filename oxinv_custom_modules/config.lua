Config = Config or {}
Config.Modules = Config.Modules or {}

-- Master module toggles are stored inside each module config.
-- Edit files under modules/<module_name>/config.lua for per-module behavior.
